apiVersion: v1
kind: Service
metadata:
  name: hytale-server
  namespace: hytale
spec:
  selector:
    app: hytale
    role: server
  type: ClusterIP
  ports:
    - name: hytale-udp
      port: 5520
      targetPort: 5520
      protocol: UDP
